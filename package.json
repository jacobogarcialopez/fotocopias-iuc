{
  "name": "iuc-monedero-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "tailwindcss": "^4.3.3"
  }
}
